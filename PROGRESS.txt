## Session: Implement blacksmith progress command (simple-agent-harness-c34r)

### Completed
- Added first-class `blacksmith progress` CLI with subcommands:
  - `blacksmith progress add`
  - `blacksmith progress list`
  - `blacksmith progress show`
- Implemented SQLite-backed storage in `progress_entries` table.
- Added multiline markdown support for progress input (`--stdin`, `--file`, and `--body`).
- Added missing-bead behavior: warns when `--bead-id` is omitted but still persists entry.
- Wired command to runtime shared data-dir resolution so main checkout and worktrees use shared DB.
- Updated operator guidance in `PROMPT.md` and `templates/PROMPT.md.tmpl` to remove PROGRESS file workflow references.
- Removed legacy `PROGRESS.txt` / `PROGRESS_LOG.txt` handling from `blacksmith finish`.

### Verification run
- `blacksmith --help | rg '^  progress\b'`
- `blacksmith progress --help`
- `printf '## Handoff\n- Completed X\n- Next Y\n' | blacksmith progress add --bead-id simple-agent-harness-c34r --stdin`
- `blacksmith progress show --bead-id simple-agent-harness-c34r`
- `blacksmith progress list --bead-id simple-agent-harness-c34r --last 5`
- `blacksmith progress add --stdin <<< 'entry without bead id'`
- `rg -n 'Read PROGRESS.txt|write PROGRESS.txt|PROGRESS_LOG.txt' PROMPT.md templates/PROMPT.md.tmpl`
- `cargo test --release`
- `cargo clippy --fix --allow-dirty`
- `cargo fmt --check` (failed due formatting; then applied `cargo fmt`)

### Current State
- All tests pass in release mode.
- Lint ran successfully with fixes.
- Files are formatted after `cargo fmt`.
- Bead is ready to close via `blacksmith finish`.

### Suggested Next Task
- If any follow-up is needed, monitor for the existing harness.toml deprecation warning and migrate to `.blacksmith/config.toml` project-wide.
