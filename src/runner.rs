/// Core loop: dispatch sessions, monitor, collect metrics, repeat.
///
/// The runner orchestrates the main iteration loop, coordinating
/// session spawning, watchdog monitoring, retry logic, and hook execution.
pub struct Runner {
    // TODO: config, state, metrics collector
}

impl Runner {
    // TODO: pub async fn run(&mut self) -> Result<(), Box<dyn std::error::Error>>
}
